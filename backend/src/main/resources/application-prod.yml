server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 8080
spring:
  devtools:
    restart:
      enabled: true
      additional-paths:
        - "."
    livereload:
      enabled: true
      trigger-file: .restart
    remote:
      secret: mysecret
  datasource:
    url: jdbc:mysql://opensw.chom8scy20q0.ap-northeast-2.rds.amazonaws.com
    username: root
    password: plogging2021112828
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    path match:
      matching-strategy: ant_path_matcher

logging:
  level:
    "[com.example.server]": INFO
    "[org.hibernate.type]": TRACE
greeting:
  message: this is dev application.yml